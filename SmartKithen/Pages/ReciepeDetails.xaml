<Page x:Class="SmartKithen.Pages.RecipeDetails"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:SmartKithen.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="1330" d:DesignWidth="800"
      Title="RecipeDetails"
      Background="#FFF8FC"
      MinWidth="800"
      MinHeight="600">

    <Page.Resources>
        <Style x:Key="BackButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#1A5D34"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                CornerRadius="8"
                                Padding="{TemplateBinding Padding}">
                            <Border.Effect>
                                <DropShadowEffect BlurRadius="8" Opacity="0.2" ShadowDepth="2"/>
                            </Border.Effect>
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#144528"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SecondaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="#1A5D34"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#1A5D34"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#F0F9FF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ActionButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#1A5D34"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#FFF0F5"/>
                                <Setter Property="Foreground" Value="#CFA1C1"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="GreenButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#1A5D34"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="20,12"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                CornerRadius="8"
                                Padding="{TemplateBinding Padding}">
                            <Border.Effect>
                                <DropShadowEffect BlurRadius="8" Opacity="0.2" ShadowDepth="2"/>
                            </Border.Effect>
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#144528"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="CornerRadius" Value="15"/>
            <Setter Property="Padding" Value="20"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect BlurRadius="10" Opacity="0.1" ShadowDepth="3"/>
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>

    <Grid>
        <Rectangle>
            <Rectangle.Fill>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                    <GradientStop Color="#FFF8FC" Offset="0"/>
                    <GradientStop Color="#FFAC6586" Offset="1"/>
                </LinearGradientBrush>
            </Rectangle.Fill>
        </Rectangle>

        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Margin="0,0,0,20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <Button Grid.Column="0" x:Name="BackButton" Content="← Назад" Style="{StaticResource BackButtonStyle}" Click="BackButton_Click"/>

                <WrapPanel Grid.Column="2" HorizontalAlignment="Right" Margin="10,0,0,0">
                    <Button x:Name="AddToFavoritesButton" Content="❤️ В избранное" Style="{StaticResource ActionButtonStyle}" ToolTip="Добавить в избранное" Click="AddToFavoritesButton_Click"/>
                    <Button x:Name="ShareButton" Content="📤 Поделиться" Style="{StaticResource ActionButtonStyle}" ToolTip="Поделиться рецептом" Margin="10,0,0,0" Click="ShareButton_Click"/>
                    <Button x:Name="PrintButton" Content="🖨️ Печать" Style="{StaticResource ActionButtonStyle}" ToolTip="Распечатать рецепт" Margin="10,0,0,0" Click="PrintButton_Click"/>
                </WrapPanel>
            </Grid>

            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                <StackPanel Orientation="Vertical">

                    <Grid Margin="0,0,0,30">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="300"/>
                            <ColumnDefinition Width="20"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <Border Grid.Column="0" Height="300" Background="White" CornerRadius="20" BorderBrush="#E0E0E0" BorderThickness="1">
                            <Border.Effect>
                                <DropShadowEffect BlurRadius="10" Opacity="0.1" ShadowDepth="3"/>
                            </Border.Effect>
                            <Grid>
                                <Image x:Name="RecipeImage" Height="270" Stretch="Uniform"/>
                            </Grid>
                        </Border>

                        <StackPanel Grid.Column="2" VerticalAlignment="Top">
                            <TextBlock x:Name="RecipeTitleText" FontSize="32" FontWeight="Bold" Foreground="#1A5D34" TextWrapping="Wrap" Margin="0,0,0,15"/>

                            <WrapPanel Margin="0,0,0,15">
                                <Border Background="#FFF8FC" CornerRadius="10" Padding="12,8" Margin="0,0,10,5">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="⏱️" FontSize="16" Margin="0,0,8,0"/>
                                        <TextBlock x:Name="CookingTimeText" FontSize="14" Foreground="#1A5D34" FontWeight="Medium"/>
                                    </StackPanel>
                                </Border>

                                <Border Background="#FFF8FC" CornerRadius="10" Padding="12,8" Margin="0,0,10,5">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="🍽️" FontSize="16" Margin="0,0,8,0"/>
                                        <TextBlock x:Name="CategoryText" FontSize="14" Foreground="#1A5D34" FontWeight="Medium"/>
                                    </StackPanel>
                                </Border>

                                <Border x:Name="MealCategoryBorder" Background="#FFF8FC" CornerRadius="10" Padding="12,8" Margin="0,0,10,5" Visibility="Collapsed">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock x:Name="MealCategoryIcon" FontSize="16" Margin="0,0,8,0"/>
                                        <TextBlock x:Name="MealCategoryText" FontSize="14" Foreground="#1A5D34" FontWeight="Medium"/>
                                    </StackPanel>
                                </Border>
                            </WrapPanel>

                            <Border Background="#F9F9F9" CornerRadius="10" Padding="15" Margin="0,0,0,15">
                                <TextBlock x:Name="RecipeDescriptionText" FontSize="15" Foreground="#555" TextWrapping="Wrap" LineHeight="22"/>
                            </Border>

                            <WrapPanel>
                                <Button x:Name="StartCookingButton" Content="🍳 Начать готовку" Style="{StaticResource GreenButtonStyle}" Width="200" Margin="0,0,10,0" Click="StartCookingButton_Click"/>
                                <Button x:Name="AddRecipeToShoppingListButton" Content="🛒 В список покупок" Style="{StaticResource SecondaryButtonStyle}" Padding="15,12" Click="AddRecipeToShoppingListButton_Click"/>
                            </WrapPanel>
                        </StackPanel>
                    </Grid>

                    <Border Style="{StaticResource CardStyle}" Margin="0,0,0,20">
                        <StackPanel Orientation="Vertical">
                            <Grid Margin="0,0,0,20">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0" Orientation="Horizontal">
                                    <TextBlock Text="📝 ИНГРЕДИЕНТЫ" FontSize="20" FontWeight="Bold" Foreground="#1A5D34" VerticalAlignment="Center"/>
                                    <TextBlock Text=" (отметьте для покупки)" FontSize="14" Foreground="#666" VerticalAlignment="Center" Margin="10,0,0,0"/>
                                </StackPanel>
                                <Button x:Name="AddSelectedToShoppingListButton" Grid.Column="1" Content="🛒 Добавить отмеченное" Style="{StaticResource SecondaryButtonStyle}" Padding="15,8" FontSize="12" Click="AddSelectedToShoppingListButton_Click" Visibility="Collapsed"/>
                            </Grid>
                            <ScrollViewer MaxHeight="400" VerticalScrollBarVisibility="Auto">
                                <StackPanel x:Name="IngredientsPanel" Orientation="Vertical" Margin="0,0,10,0"/>
                            </ScrollViewer>
                            <Border Background="#FFF8FC" CornerRadius="10" Padding="15" Margin="0,20,0,0">
                                <TextBlock Text="💡 Выберите продукты, которые нужно купить, и нажмите кнопку выше" FontSize="13" Foreground="#666" TextAlignment="Center"/>
                            </Border>
                        </StackPanel>
                    </Border>

                    <Border Style="{StaticResource CardStyle}">
                        <StackPanel Orientation="Vertical">
                            <TextBlock Text="👩‍🍳 ШАГИ ПРИГОТОВЛЕНИЯ" FontSize="20" FontWeight="Bold" Foreground="#1A5D34" Margin="0,0,0,20"/>
                            <StackPanel x:Name="StepsPanel" Orientation="Vertical"/>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </ScrollViewer>
        </Grid>
    </Grid>
</Page>